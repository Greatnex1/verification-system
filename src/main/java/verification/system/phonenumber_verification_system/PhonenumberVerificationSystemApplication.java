package verification.system.phonenumber_verification_system;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class PhonenumberVerificationSystemApplication {

	public static void main(String[] args) {
		SpringApplication.run(PhonenumberVerificationSystemApplication.class, args);
	}

//	System.getenv("TWILIO_ACCOUNT_SID")System.getenv("TWILIO_AUTH_TOKEN")

}
